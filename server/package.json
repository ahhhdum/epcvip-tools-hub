{
  "name": "epcvip-tools-hub-server",
  "version": "1.0.0",
  "description": "Multiplayer server for EPCVIP Tools Hub game",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc && npm run copy-static",
    "copy-static": "mkdir -p public && cp -r ../index.html ../index-simple.html ../style.css ../js ../assets ../maps ../tools ../wordle ./public/",
    "serve": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "http-proxy-middleware": "^3.0.5",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/http-proxy-middleware": "^0.19.3",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
